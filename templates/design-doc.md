<!--
汎用・設計ドキュメント雛形（散文＝md）。設計ノード(wf-design/wf-designer)が大きめの設計を残す時に使う。
通常の状態・チェック・受入基準は status.json（JSON）が真実源。本雛形は「合意のための散文設計」を補う。
スタック固有の判断は .claude/rules/CODING.md と .claude/docs/DESIGN.md に従う。
-->

# 設計: <タスク名>（#NN / R-NN）

| 項目 | 値 |
|---|---|
| 関連 Issue / 要件 | #NN / R-NN / SPEC §x |
| 影響 component | <config.components のどれか（例 backend / app）> |

## 目的・背景
<なぜやるか。解こうとしている問題・期待結果を 1〜3 文で>

## スコープ
- **やる**: <含む範囲>
- **やらない**: <除外して別 Issue にする範囲>

## 方針（既存の再利用を優先）
- <採用するアプローチ。既存パターン/共通部品/サービスの再利用点を明記（新規より流用）>
- <層責務・契約・mock-first 等、rules/CODING に沿う設計判断>

## 影響範囲（変更ファイル）
- `<path>` — <何を変えるか>

## 受入基準（観測可能・検証ノードが検査）
- [ ] <観測可能な条件1>
- [ ] <観測可能な条件2>

## リスク・未確定
- <技術リスク / コスト（予算超過の恐れは PM へエスカレーション）/ 後で決めること>

## 遷移・データ・契約（必要なら）
```mermaid
flowchart LR
  A[<入力>] --> B[<処理>] --> C[<出力>]
```
