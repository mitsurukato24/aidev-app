# DATA_POLICY ルール — データとコンテンツの決まり

<!-- project-init が生成する骨組み。見出し構成は維持し、{{...}} とコメントの箇所に
     このプロジェクトのデータ/コンテンツ固有規約を書く。データを扱わないプロジェクトは大幅に簡略化してよい。 -->

## 真実源（Source of Truth）

{{SOURCE_OF_TRUTH}}
<!-- このプロジェクトの主要データ/コンテンツの真実源(どのファイル/DB が正か)を列挙。例:
- 設定/シードデータ: backend/data/... が真実源、起動時に冪等投入。
- ユーザーデータ: 端末ローカル DB を単一の真実源にしてクラウドと同期。
真実源が単純なら 1〜2 行でよい。 -->

## コスト方針

{{COST_POLICY}}
<!-- 生成/外部 API などコストの発生する処理の方針を書く。例:
- 高コストな生成は一度だけ行い共有する（per-user コスト≈0）。
- 予算を超えるコストが発生しうる作業は着手前にユーザー判断を仰ぐ（PM 判断ポイント）。
コストの懸念が無いプロジェクトはこの節を削除してよい。 -->

## {{LICENSE_TITLE}}

{{LICENSE_RULES}}
<!-- 外部ソース/コンテンツのライセンス順守ルールがあれば書く（例: 逐語コピー禁止・言い換え・出典必須・
     ライセンス種別ごとの扱い）。serve 時ゲート等で強制する場合はその所在も。該当しなければ削除。 -->

## 保存とプライバシー

{{STORAGE_PRIVACY}}
<!-- 永続化先・同期方針・個人データの最小取得/保持などを書く。例:
- 端末ローカルが単一の真実源、クラウドと同期。
- ユーザー個人データは最小限の取得・保持に留める。 -->
