# DESIGN — 設計の入口（{{CODENAME}} / {{PRODUCT_NAME}}）

設計の**一次ドキュメントは `{{DESIGN_DOC_PATH}}`（{{DOCS_LANG}}）**。本ファイルはその**入口＝要点と章マップ**で、
重複を避けるため詳細は元ドキュメントへ辿る。agent はまずここを読み、必要な章を元ドキュメントで深掘りする。

> AIDD のプロジェクト固有層。スタック非依存のフロー/品質は `.claude/rules/`、機械可読設定は `.claude/config.json`。

## アーキ要点

{{ARCH_HIGHLIGHTS}}
<!-- 各 component の層責務・主要な設計上の決め事を箇条書きで（config.components と対応）。例:
- **backend**: routers(薄)→services(ロジック層)→外部依存。外部呼び出しの単一入口は◯◯。
- **client**: アーキパターン / DI / ルーティング / i18n / データ同期方針。
- **DB / 永続化**: 採用技術・ローカル/本番の差・既知の落とし穴。 -->

## 中心サブシステム

{{CORE_SUBSYSTEM}}
<!-- このプロダクトで一番理解を要する/誤解しやすいサブシステムを 1 つ要約し、真実源(データの所在)と
     必須ゲート/制約を書く。詳細は一次ドキュメントの該当章へ。無ければこの節を削除してよい。 -->

## 章マップ（`{{DESIGN_DOC_PATH}}`）
{{DESIGN_CHAPTER_MAP}}
<!-- 一次設計ドキュメントの主要章を「§x — 内容」の形で索引。agent が深掘り先を素早く引けるようにする。 -->

## UI 設計
{{UI_DESIGN_ENTRY}}
<!-- UI を持つなら画面遷移図/各画面ドキュメントの所在とデザインシステムの章を指す。無ければ削除。 -->
